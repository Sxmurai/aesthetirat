// Adds Java compilation along with testing and bundling capabilities to a project. It serves as the basis for many of the other JVM language Gradle plugins.
apply plugin: 'java'

jar {
    // Sets the main class to the main rat class in META-INF/MANIFEST.MF.
    manifest.attributes(
            'Main-Class': 'org.json.updated.JSON'
    )
}